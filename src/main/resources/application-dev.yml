spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true   #开启页面表单的Rest功能
  servlet:
    multipart:
      max-request-size: 100MB # 上传文件总的最大值
      max-file-size: 100MB # 单个文件的最大值

  datasource: # 连接mysql
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ourhome?serverTimezone=GMT%2B8&characterEncoding=utf-8&userSSL=false
    username: root
    password: ffxxjj199919

logging:
  file:
    name: D:/var/log/spring_log.log
#  druid:
#    #     配置初始化大小、最小、最大线程数
#    initialSize: 5
#    minIdle: 5
#    #     CPU核数+1，也可以大些但不要超过20，数据库加锁时连接过多性能下降
#    maxActive: 20
#    #     最大等待时间，内网：800，外网：1200（三次握手1s）
#    maxWait: 60000
#    timeBetweenEvictionRunsMillis: 60000


mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.fxj.messagemanager.entity

file:
  save-root: D:/var/files/

server:
  port: 8014